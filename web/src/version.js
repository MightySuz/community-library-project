// Simple build version marker. The timestamp is evaluated at build time.
// Search this string in the deployed bundle / browser console to confirm the build is up to date.
export const BUILD_VERSION = `build-${new Date().toISOString()}`;
